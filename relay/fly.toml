# fly.toml app configuration file generated for localflight-community-relay on 2026-04-28T04:48:43+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'localflight-community-relay'
primary_region = 'fra'

[build]
  dockerfile = 'Dockerfile'

[env]
  DB_PATH = '/data/relay.db'
  PORT = '8080'
  RELAY_COMMUNITY_SCHEDULE_LIMIT = '50'
  RELAY_PUBLIC_HOST = 'relay.localflight.app'
  RELAY_ADMIN_HOST = 'network.localflight.app'

[[mounts]]
  source = 'relay_data'
  destination = '/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '512mb'
  cpus = 1
  memory_mb = 512
