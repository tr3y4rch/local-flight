<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/widget_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@drawable/localflight_widget_background"
  android:orientation="vertical"
  android:padding="12dp">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
      android:id="@+id/widget_airport"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_weight="1"
      android:ellipsize="end"
      android:fontFamily="monospace"
      android:maxLines="1"
      android:text="LOCAL FLIGHT"
      android:textColor="@color/localflight_widget_text"
      android:textSize="14sp"
      android:textStyle="bold" />

    <TextView
      android:id="@+id/widget_refresh"
      android:layout_width="40dp"
      android:layout_height="40dp"
      android:gravity="center"
      android:text="↻"
      android:textColor="@color/localflight_widget_blue"
      android:textSize="20sp"
      android:contentDescription="Refresh Local Flight widget" />
  </LinearLayout>

  <TextView
    android:id="@+id/widget_freshness"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:ellipsize="end"
    android:fontFamily="monospace"
    android:maxLines="1"
    android:text="OPEN APP"
    android:textColor="@color/localflight_widget_green"
    android:textSize="9sp"
    android:textStyle="bold" />

  <View
    android:layout_width="match_parent"
    android:layout_height="1dp"
    android:layout_marginBottom="5dp"
    android:layout_marginTop="5dp"
    android:background="@color/localflight_widget_separator" />

  <TextView
    android:id="@+id/widget_row_1"
    style="@style/LocalFlightWidgetRow" />

  <TextView
    android:id="@+id/widget_row_2"
    style="@style/LocalFlightWidgetRow" />

  <TextView
    android:id="@+id/widget_row_3"
    style="@style/LocalFlightWidgetRow" />

  <TextView
    android:id="@+id/widget_empty"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:gravity="center_vertical"
    android:maxLines="2"
    android:text="Open Local Flight to prepare the board"
    android:textColor="@color/localflight_widget_muted"
    android:textSize="12sp" />
</LinearLayout>
